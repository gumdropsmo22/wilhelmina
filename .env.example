# Wilhelmina — example environment (C4)
# Copy to .env and fill in values; never commit real secrets.

# Discord
DISCORD_TOKEN=changeme

# OpenAI (wired later in C10)
OPENAI_API_KEY=

# Database (wired later; placeholder only)
MONGO_URL=

# Locale/timezone (used when we later schedule jobs)
TZ=Asia/Riyadh
